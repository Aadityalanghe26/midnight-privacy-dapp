# Tests

Contract tests live in `contracts/counter/counter.test.ts` and are run via the workspace vitest config.

Frontend component tests live in `frontend/src/components/`.

Run everything from the repo root:

```bash
npm test
```
